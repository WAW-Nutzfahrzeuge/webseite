<section class="process-section section" id="ablauf">
    <div class="container process-layout">
        <div class="process-copy glass-reflect" data-animate>
            <span class="badge">Ablauf</span>
            <h2>So einfach verkaufen Sie Ihr Nutzfahrzeug.</h2>
            <p>
                Der Verkaufsprozess ist bewusst schlank gehalten: Daten senden,
                Rückmeldung erhalten und die weitere Abwicklung direkt mit WAW abstimmen.
            </p>

            <a href="/kontakt" class="btn btn-primary">Anfrage starten</a>
            <a href="/ablauf" class="btn btn-secondary">Ablauf im Detail →</a>
        </div>

        <div class="process-steps">
            <article class="process-step glass-reflect" data-animate>
                <div class="step-icon">
                    <svg viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M4 4h16v16H4z" />
                        <path d="M8 8h8" />
                        <path d="M8 12h8" />
                        <path d="M8 16h5" />
                    </svg>
                </div>

                <span>01</span>

                <div>
                    <h3>Fahrzeugdaten senden</h3>
                    <p>Marke, Modell, Baujahr, Zustand und optional Bilder übermitteln.</p>
                </div>
            </article>

            <article class="process-step glass-reflect" data-animate>
                <div class="step-icon">
                    <svg viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M12 3v18" />
                        <path d="M4 8h16" />
                        <path d="M6 16h12" />
                        <path d="M8 21h8" />
                    </svg>
                </div>

                <span>02</span>

                <div>
                    <h3>Rückmeldung erhalten</h3>
                    <p>Wir melden uns schnellstmöglich zur Abstimmung.</p>
                </div>
            </article>

            <article class="process-step glass-reflect" data-animate>
                <div class="step-icon">
                    <svg viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M8 12h8" />
                        <path d="M12 8v8" />
                        <path d="M4 6h16v12H4z" />
                    </svg>
                </div>

                <span>03</span>

                <div>
                    <h3>Angebot abstimmen</h3>
                    <p>Details werden transparent besprochen — ohne unnötige Umwege.</p>
                </div>
            </article>

            <article class="process-step glass-reflect" data-animate>
                <div class="step-icon">
                    <svg viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M20 6 9 17l-5-5" />
                    </svg>
                </div>

                <span>04</span>

                <div>
                    <h3>Verkauf abwickeln</h3>
                    <p>Nach Einigung erfolgt die professionelle Abwicklung und Übergabe.</p>
                </div>
            </article>
        </div>
    </div>
</section>

<style>
    .process-layout {
        display: grid;
        grid-template-columns: 0.9fr 1.1fr;
        gap: 34px;
        align-items: start;
    }

    .process-copy {
        position: sticky;
        top: 120px;

        padding: 38px;
        border-radius: 38px;

        background:
                radial-gradient(circle at top right, rgba(255,107,0,0.22), transparent 18rem),
                linear-gradient(135deg, var(--navy-deep), var(--navy-soft));

        color: white;
        box-shadow: var(--shadow);
        overflow: hidden;
    }

    .process-copy .badge {
        background: rgba(255,255,255,0.12);
        color: rgba(255,255,255,0.82);
        border-color: rgba(255,255,255,0.18);
    }

    .process-copy h2 {
        margin: 18px 0 0;
        font-size: clamp(36px, 4.5vw, 66px);
        line-height: 0.98;
        letter-spacing: -0.07em;
        color: white;
    }

    .process-copy p {
        margin-top: 18px;
        color: rgba(255,255,255,0.68);
        font-size: 18px;
        line-height: 1.7;
    }

    .process-copy .btn {
        margin-top: 24px;
    }

    .process-copy .btn + .btn {
        margin-top: 10px;
    }

    .process-copy .btn-primary {
        background: linear-gradient(135deg, var(--orange), var(--orange-dark));
        color: white;
    }

    .process-copy .btn-secondary {
        background: rgba(255,255,255,0.10);
        color: rgba(255,255,255,0.88);
        border-color: rgba(255,255,255,0.18);
    }

    .process-copy .btn-secondary:hover {
        background: rgba(255,255,255,0.16);
    }

    .process-steps {
        position: relative;
        display: grid;
        gap: 16px;
    }

    .process-steps::before {
        content: "";
        position: absolute;
        left: 39px;
        top: 28px;
        bottom: 28px;
        width: 2px;
        background:
                linear-gradient(
                        180deg,
                        rgba(15,23,42,0),
                        rgba(15,23,42,0.16),
                        rgba(15,23,42,0)
                );
        z-index: 0;
    }

    .process-step {
        position: relative;
        z-index: 1;

        display: grid;
        grid-template-columns: 1fr;
        gap: 22px;

        padding: 30px;
        border-radius: 32px;

        background: rgba(255,255,255,0.8);
        border: 1px solid var(--card-border);
        box-shadow: var(--shadow);
        overflow: hidden;

        transition:
                transform 280ms cubic-bezier(0.16, 1, 0.3, 1),
                background 280ms ease,
                border-color 280ms ease,
                box-shadow 280ms ease;
    }

    .process-step > .step-icon,
    .process-step > span {
        grid-row: 1;
    }

    .process-step > .step-icon {
        justify-self: start;
    }

    .process-step > span {
        justify-self: end;
    }

    .process-step > div:last-child {
        max-width: 620px;
    }

    .process-step:hover {
        transform: translateX(8px);
        background: #ffffff;
        border-color: rgba(15,23,42,0.14);
    }

    .process-step::before {
        content: "";
        position: absolute;
        width: 160px;
        height: 160px;
        right: -70px;
        top: -70px;
        border-radius: 999px;
        background: radial-gradient(circle, rgba(15,23,42,0.08), transparent 68%);
        opacity: 0;
        transition: 280ms ease;
        pointer-events: none;
    }

    .process-step:hover::before {
        opacity: 1;
    }

    .step-icon {
        width: 64px;
        height: 64px;

        display: grid;
        place-items: center;

        border-radius: 22px;

        background: linear-gradient(135deg, var(--orange), var(--orange-dark));
        color: white;

        box-shadow:
                0 16px 36px rgba(15,23,42,0.12);

        transition:
                transform 280ms cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 280ms ease;
    }

    .process-step:hover .step-icon {
        transform: scale(1.06) rotate(-2deg);
        box-shadow:
                0 18px 42px rgba(15,23,42,0.16);
    }

    .step-icon svg {
        width: 28px;
        height: 28px;

        fill: none;
        stroke: currentColor;
        stroke-width: 2.2;
        stroke-linecap: round;
        stroke-linejoin: round;
    }

    .process-step span {
        width: 46px;
        height: 46px;

        border-radius: 16px;

        display: grid;
        place-items: center;

        background: rgba(15,23,42,0.055);
        border: 1px solid rgba(15,23,42,0.07);

        color: var(--text);

        font-weight: 900;
        letter-spacing: -0.05em;
    }

    .process-step h3 {
        margin: 0 0 10px;
        font-size: 25px;
        letter-spacing: -0.05em;
    }

    .process-step p {
        margin: 0;
        color: var(--muted);
        font-size: 17px;
        line-height: 1.65;
    }

    @media (max-width: 900px) {
        .process-layout {
            grid-template-columns: 1fr;
        }

        .process-copy {
            position: static;
        }
    }

    @media (max-width: 640px) {
        .process-step {
            grid-template-columns: 58px 1fr;
            gap: 14px;
            padding: 20px;
        }

        .process-step span {
            display: none;
        }

        .step-icon {
            width: 54px;
            height: 54px;
            border-radius: 18px;
        }

        .step-icon svg {
            width: 24px;
            height: 24px;
        }

        .process-steps::before {
            left: 28px;
        }
    }
</style>
