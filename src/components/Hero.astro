<section class="hero">
  <div class="hero-overlay"></div>

  <div class="container hero-inner">
    <div class="hero-content">
      <p class="hero-kicker">LKW ANKAUF DEUTSCHLAND- UND EUROPAWEIT</p>

      <h1>LKW & Nutzfahrzeuge verkaufen.</h1>

      <p class="hero-text">
        Wir kaufen LKW, Sattelzugmaschinen, Auflieger, Anhänger, Transporter,
        Baumaschinen und Fuhrparks direkt an.
      </p>

      <div class="hero-actions">
        <a href="/kontakt" class="btn btn-primary">Fahrzeug anbieten</a>
        <a href="/leistungen" class="btn btn-ghost">Leistungen ansehen</a>
      </div>

      <div class="hero-trust">
        <span>Faire Preise</span>
        <span>Schnelle Abwicklung</span>
        <span>Deutschland- und europaweit</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 760px;
    padding: 170px 0 110px;
    color: white;
    overflow: hidden;

    background-image: url("/images/hero-yard-sunset.png");
    background-size: cover;
    background-position: center right;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background:
        linear-gradient(90deg, rgba(0,0,0,0.62) 0%, rgba(0,0,0,0.25) 46%, rgba(0,0,0,0.08) 100%),
        linear-gradient(180deg, rgba(0,0,0,0.12), rgba(0,0,0,0.22));
    z-index: 0;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
  }

  .hero-content {
    max-width: 720px;
  }

  .hero-kicker {
    margin: 0 0 18px;
    color: var(--orange);
    font-size: 15px;
    font-weight: 900;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0;
    max-width: 760px;
    font-size: clamp(52px, 6vw, 96px);
    line-height: 0.92;
    letter-spacing: -0.075em;
    color: white;
  }

  .hero-text {
    max-width: 620px;
    margin: 26px 0 0;
    color: rgba(255,255,255,0.82);
    font-size: clamp(18px, 1.7vw, 22px);
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    gap: 14px;
    margin-top: 32px;
  }

  .btn-ghost {
    background: rgba(255,255,255,0.08);
    color: white;
    border: 1px solid rgba(255,255,255,0.36);
  }

  .btn-ghost:hover {
    background: rgba(255,255,255,0.16);
  }

  .hero-trust {
    display: flex;
    flex-wrap: wrap;
    gap: 28px;
    margin-top: 42px;
    color: rgba(255,255,255,0.9);
    font-size: 15px;
    font-weight: 800;
  }

  .hero-trust span::before {
    content: "✓";
    margin-right: 8px;
    color: var(--orange);
  }

  @media (max-width: 760px) {
    .hero {
      min-height: 860px;
      padding: 120px 0 70px;
      background-image: url("/images/hero-yard-sunset-mobile.png");
      background-size: cover;
      background-position: center top;
    }

    .hero-overlay {
      background:
          linear-gradient(180deg, rgba(0,0,0,0.38), rgba(0,0,0,0.56)),
          linear-gradient(90deg, rgba(0,0,0,0.52), rgba(0,0,0,0.1));
    }

    .hero-content {
      max-width: 100%;
    }

    h1 {
      font-size: clamp(44px, 13vw, 62px);
    }

    .hero-actions {
      flex-direction: column;
    }

    .hero-trust {
      flex-direction: column;
      gap: 12px;
      margin-top: 28px;
    }
  }
</style>
