<section class="seo-content section">
    <div class="container seo-layout">
        <div class="seo-sticky" data-animate>
            <span class="badge">Nutzfahrzeughandel</span>

            <h2>
                LKW Ankauf & Nutzfahrzeughandel in Deutschland.
            </h2>

            <p>
                WAW ist spezialisiert auf den Ankauf und Verkauf von Nutzfahrzeugen,
                LKW, Aufliegern, Anhängern und Baumaschinen.
            </p>
        </div>

        <div class="seo-text" data-animate>
            <p>
                Unternehmen, Speditionen und Händler suchen häufig nach einem
                professionellen Ansprechpartner, wenn gebrauchte Nutzfahrzeuge,
                LKW oder Baumaschinen verkauft werden sollen. WAW unterstützt
                beim Ankauf und Verkauf von gewerblich genutzten Fahrzeugen und
                bietet eine schnelle und direkte Kommunikation.
            </p>

            <p>
                Angefragt werden unter anderem Sattelzugmaschinen, Auflieger,
                Anhänger, Transporter, Baumaschinen und weitere Nutzfahrzeuge.
                Auch Fahrzeuge mit hoher Laufleistung oder ältere Fahrzeuge
                können angefragt werden.
            </p>

            <p>
                Durch regionale Landingpages wie „LKW verkaufen Hamburg“,
                „LKW verkaufen Berlin“ oder „LKW verkaufen Hannover“ wird
                eine bessere regionale Auffindbarkeit ermöglicht.
            </p>

            <p>
                Wir sind deutschland- und europaweit tätig und richten uns an Unternehmen,
                Fuhrparks, Händler und Gewerbetreibende, die Nutzfahrzeuge
                professionell verkaufen möchten.
            </p>
        </div>
    </div>
</section>

<style>
    .seo-layout {
        display: grid;
        grid-template-columns: 0.85fr 1.15fr;

        gap: 34px;

        align-items: start;
    }

    .seo-sticky {
        position: sticky;
        top: 120px;

        padding: 34px;

        border-radius: 36px;

        background:
                linear-gradient(
                        135deg,
                        #f26100,
                        #b83c00
                );

        color: white;

        box-shadow: var(--shadow);
    }

    .seo-sticky .badge {
        background: rgba(255,255,255,0.12);

        color: rgba(255,255,255,0.82);

        border-color: rgba(255,255,255,0.16);
    }

    .seo-sticky h2 {
        margin:
                18px 0
                0;

        font-size: clamp(38px, 5vw, 66px);

        line-height: 0.96;

        letter-spacing: -0.07em;

        color: white;
    }

    .seo-sticky p {
        margin-top: 18px;

        color: rgba(255,255,255,0.68);

        font-size: 18px;

        line-height: 1.7;
    }

    .seo-text {
        display: grid;

        gap: 18px;
    }

    .seo-text p {
        margin: 0;

        padding: 28px;

        border-radius: 28px;

        background: rgba(255,255,255,0.78);

        border: 1px solid var(--card-border);

        color: var(--muted);

        font-size: 17px;

        line-height: 1.85;

        box-shadow: var(--shadow);

        transition:
                transform 240ms ease,
                background 240ms ease;
    }

    .seo-text p:hover {
        transform: translateY(-4px);

        background: #ffffff;
    }

    @media (max-width: 900px) {
        .seo-layout {
            grid-template-columns: 1fr;
        }

        .seo-sticky {
            position: static;
        }
    }
</style>
